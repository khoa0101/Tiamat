const Game = require("./game.js");

class GameView extends Game{
  constructor(game, el){
    this.game = game;
    this.el = el;
    this.setupView();
  }

  setupView(){
    const grid = document.
  }
}

module.exports = GameView;